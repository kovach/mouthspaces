issues:
  - 0 length buffer issue

record audio
 [x] copy samples array

playback
 [x] audio buffer

button
 [x] start/stop/play
 - play all

[x] send to server

display messages on client
  [x] basic list
  - colored/sized divs
  - waveforms
   - http://www.waveformjs.org/ ?
   - just plot sample array?

server functions
 [x] accept connections
 [x] accept
 - validate audio blobs
   - check length
 [x] timestamp, insert blobs into list
 - delete old blobs

 storing blobs:
  - array of [blob id, blob data, timestamp, connection id]
    - sorted by timestamp

low priority:

audio processing?
  - noise reduction?
