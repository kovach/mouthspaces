https://developer.mozilla.org/en-US/docs/Web/API/AudioContext

record audio
 [x] manually copy samples array
  - chrome issue??

playback
 [x] audio buffer

button
 [x] start/stop/play
 - play all

[x] send to server


display waveform
 - http://www.waveformjs.org/ ?
 - just plot sample array?

server functions
 [x] accept connections
 [x] accept
 - validate audio blobs
   - check length
 - timestamp, insert blobs into list
 - delete old blobs

 storing blobs:
  - array of [blob id, blob data, timestamp, connection id]
    - sorted by timestamp
